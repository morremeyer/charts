apiVersion: v1
kind: Service
metadata:
  name: {{ include "postfix-sendonly.fullname" . }}
  labels:
    {{- include "postfix-sendonly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: smtp
      protocol: TCP
      name: smtp
  selector:
    {{- include "postfix-sendonly.selectorLabels" . | nindent 4 }}
